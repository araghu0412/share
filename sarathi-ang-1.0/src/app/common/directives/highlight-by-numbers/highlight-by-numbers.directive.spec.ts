import { HighlightByNumbersDirective } from './highlight-by-numbers.directive';

describe('HighlightByNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightByNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
